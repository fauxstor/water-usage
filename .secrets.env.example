# Copy to .secrets.env (gitignored) and fill in your getMyMeter.info credentials.
# Used only by local probe scripts — Home Assistant stores credentials in its config entry.

GETMYMETER_USER=your_username
GETMYMETER_PASSWORD=your_password

# Optional overrides
# GETMYMETER_BASE=https://h2o-analytics-hrd.appspot.com
